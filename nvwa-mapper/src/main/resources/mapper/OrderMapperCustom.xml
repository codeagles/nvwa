<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.codeagles.mapper.OrderMapperCustom" >

  <resultMap id="myOrdersVO"  type="com.codeagles.vo.MyOrdersVO">
    <id column="orderId" property="orderId" />
    <result column="createdTime" property="createdTime" />
    <result column="payMethod" property="payMethod" />
    <result column="realPayAmount" property="realPayAmount" />
    <result column="postAmount" property="postAmount" />
    <result column="orderStatus" property="orderStatus" />

    <collection property="subOrderItemList" select="getSubItems"
                column="orderId"
                ofType="com.codeagles.vo.MySubOrderItemVO">
      <result column="itemId" property="itemId" />
      <result column="itemImg" property="itemImg" />
      <result column="itemName" property="itemName" />
      <result column="itemSpecName" property="itemSpecName" />
      <result column="itemSpecId" property="itemSpecId" />
      <result column="buyCounts" property="buyCounts" />
      <result column="price" property="price" />
    </collection>
  </resultMap>

    <select id="queryMyOrders" parameterType="Map" resultMap="myOrdersVO">
        select
            od.id as orderId,
            od.created_time as createdTime,
            od.pay_method as payMethod,
            od.real_pay_amount as realPayAmount,
            od.post_amount as postAmount,
            os.order_status as orderStatus
        from orders od
            left JOIN order_status os on od.id = os.order_id
        where od.user_id = #{paramsMap.userId}
            <if test="paramsMap.orderStatus != null">
                and os.order_status =  #{paramsMap.orderStatus}
            </if>
            and od.is_delete = 0
        ORDER BY
        od.updated_time asc

    </select>

    <select id="getSubItems" parameterType="String" resultType="com.codeagles.vo.MySubOrderItemVO" >
        select
            oi.item_id as itemId,
            oi.item_name as itemName,
            oi.item_img as itemImg,
            oi.item_spec_id as itemSpecId,
            oi.item_spec_name as itemSpecName,
            oi.buy_counts as buyCounts,
            oi.price as price
        from order_items oi
        where oi.order_id = #{orderId}

    </select>
    <!-- 此方法多表关联嵌套 分页不准确 弃用-->
    <!--<select id="queryMyOrders" parameterType="Map" resultMap="myOrdersVO">-->
        <!--select-->
        <!--od.id as orderId,-->
        <!--od.created_time as createdTime,-->
        <!--od.pay_method as payMethod,-->
        <!--od.real_pay_amount as realPayAmount,-->
        <!--od.post_amount as postAmount,-->
        <!--os.order_status as orderStatus,-->
        <!--oi.item_id as itemId,-->
        <!--oi.item_name as itemName,-->
        <!--oi.item_img as itemImg,-->
        <!--oi.item_spec_id as itemSpecId,-->
        <!--oi.item_spec_name as itemSpecName,-->
        <!--oi.buy_counts as buyCounts,-->
        <!--oi.price as price-->
        <!--from orders od-->
        <!--left JOIN order_status os on od.id = os.order_id-->
        <!--LEFT JOIN order_items oi on od.id = oi.order_id-->
        <!--where od.user_id = #{paramsMap.userId}-->
        <!--<if test="paramsMap.orderStatus != null">-->
            <!--and os.order_status =  #{paramsMap.orderStatus}-->
        <!--</if>-->
        <!--and od.is_delete = 0-->
        <!--ORDER BY-->
        <!--od.updated_time asc-->

    <!--</select>-->
</mapper>